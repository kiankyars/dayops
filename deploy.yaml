version: "2.0"

services:
  dayops-api:
    image: kyars/dayops:latest
    expose:
      - port: 8000
        as: 80
        to:
          - global: true
    env:
      - PORT=8000
      - DAYOPS_STORAGE_ROOT=/data/dayops
      - GOOGLE_OAUTH_CLIENT_ID=replace-me
      - GOOGLE_OAUTH_CLIENT_SECRET=replace-me
      - GOOGLE_OAUTH_REDIRECT_URI=https://replace-me/auth/google/callback
      - DEFAULT_USER_TIMEZONE=America/Denver
      - GEMINI_MODELS=gemini-3-flash-preview,gemini-2.5-flash,gemini-2.5-flash-lite
      - GEMINI_API_KEY=replace-me

profiles:
  compute:
    dayops-api:
      resources:
        cpu:
          units: 0.5
        memory:
          size: 512Mi
        storage:
          size: 1Gi
  placement:
    akash:
      pricing:
        dayops-api:
          denom: uakt
          amount: 100

deployment:
  dayops-api:
    akash:
      profile: dayops-api
      count: 1
